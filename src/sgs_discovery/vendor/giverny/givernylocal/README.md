# givernylocal

Vendored local JHTDB helper package used by the reproduction environment for this repository. This copy is tracked inside the main project so the `fluid` environment can be recreated without a separate dependency checkout.
